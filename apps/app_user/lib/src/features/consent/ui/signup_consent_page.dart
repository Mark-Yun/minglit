import 'package:flutter/material.dart';

/// Placeholder for the signup consent screen.
///
/// This will be replaced by the full implementation in #881.
/// For now it provides a minimal target for the route guard redirect.
class SignupConsentPage extends StatelessWidget {
  const SignupConsentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Signup Consent — placeholder (#881)'),
      ),
    );
  }
}
