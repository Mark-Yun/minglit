import 'package:flutter/material.dart';

/// Placeholder for the partner application status page.
/// Will be fully implemented in Task 11.
class PartnerApplyStatusPage extends StatelessWidget {
  const PartnerApplyStatusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Application Status - Coming Soon')),
    );
  }
}
