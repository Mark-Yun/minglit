import 'package:flutter/material.dart';

/// Placeholder for the partner application wizard page.
/// Will be fully implemented in Task 10.
class PartnerApplyPage extends StatelessWidget {
  const PartnerApplyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Partner Apply Wizard - Coming Soon')),
    );
  }
}
