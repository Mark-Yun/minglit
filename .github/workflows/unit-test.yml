name: Unit Tests (App User)

on:
  pull_request:
    branches: [main, dev]
    paths:
      - "apps/app_user/**"
      - "shared/packages/**"
  push:
    branches: [main, dev]
    paths:
      - "apps/app_user/**"
      - "shared/packages/**"

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze (minglit_kit)
        run: flutter analyze
        working-directory: shared/packages/minglit_kit

      - name: Analyze
        run: flutter analyze
        working-directory: apps/app_user

      - name: Test
        run: flutter test
        working-directory: apps/app_user
