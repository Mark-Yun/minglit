name: shared-version-metadata

on:
  workflow_call:
    inputs:
      channel:
        description: Release channel, e.g. dev, rc, or main.
        required: true
        type: string
      source-ref:
        description: Ref or SHA to inspect.
        required: true
        type: string
    outputs:
      base_version:
        description: Platform-safe numeric version.
        value: ${{ jobs.metadata.outputs.base_version }}
      version_name:
        description: Human-facing version name.
        value: ${{ jobs.metadata.outputs.version_name }}
      build_number:
        description: Monotonic Flutter/Android/iOS build number.
        value: ${{ jobs.metadata.outputs.build_number }}
      release_tag:
        description: GitHub Release tag to publish artifacts under.
        value: ${{ jobs.metadata.outputs.release_tag }}
      release_name:
        description: GitHub Release title suffix.
        value: ${{ jobs.metadata.outputs.release_name }}
      pr_number:
        description: Version-bump PR number used as the monotonic build number.
        value: ${{ jobs.metadata.outputs.pr_number }}
      source_sha:
        description: Resolved source SHA.
        value: ${{ jobs.metadata.outputs.source_sha }}

jobs:
  metadata:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      base_version: ${{ steps.metadata.outputs.base_version }}
      version_name: ${{ steps.metadata.outputs.version_name }}
      build_number: ${{ steps.metadata.outputs.build_number }}
      release_tag: ${{ steps.metadata.outputs.release_tag }}
      release_name: ${{ steps.metadata.outputs.release_name }}
      pr_number: ${{ steps.metadata.outputs.pr_number }}
      source_sha: ${{ steps.metadata.outputs.source_sha }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.source-ref }}
          fetch-depth: 0

      - name: Compute version metadata
        id: metadata
        env:
          GH_TOKEN: ${{ github.token }}
          CHANNEL: ${{ inputs.channel }}
          SOURCE_REF: ${{ inputs.source-ref }}
        run: |
          set -euo pipefail

          case "${CHANNEL}" in
            dev|rc|main) ;;
            *)
              echo "::error::Unsupported channel '${CHANNEL}'. Expected dev, rc, or main."
              exit 1
              ;;
          esac

          SOURCE_SHA="$(git rev-parse HEAD)"
          ROOT_VERSION="$(python3 - <<'PY'
          import json
          with open('package.json') as f:
              print(json.load(f)['version'])
          PY
          )"
          BASE_VERSION="${ROOT_VERSION%-dev-staging}"
          BUMP_PR_NUM=""
          REVISION_SHA=""

          for sha in $(git rev-list --first-parent -n 80 HEAD); do
            message="$(git log -1 --format=%B "${sha}")"
            candidate="$(printf '%s\n' "${message}" | sed -nE 's/.*chore: bump version to v[0-9]{2}\.[0-9]{2}\.[0-9]{2}\+([0-9]+)-dev-staging.*/\1/p' | head -n 1)"

            if [[ "${candidate}" =~ ^[0-9]+$ ]]; then
              BUMP_PR_NUM="${candidate}"
              REVISION_SHA="${sha}"
              break
            fi
          done

          # Backward compatibility for builds cut before version-bump PR numbers
          # were embedded in the squash title.
          if [ -z "${BUMP_PR_NUM}" ]; then
            for sha in $(git rev-list --first-parent -n 80 HEAD); do
              candidate="$(gh api \
                "repos/${GITHUB_REPOSITORY}/commits/${sha}/pulls" \
                -H "Accept: application/vnd.github+json" \
                -H "X-GitHub-Api-Version: 2022-11-28" \
                --jq '.[0].number // empty' || true)"

              if [ -z "${candidate}" ]; then
                candidate="$(git log -1 --format=%B "${sha}" | sed -nE 's/.*\(#([0-9]+)\).*/\1/p' | head -n 1)"
              fi

              if [[ "${candidate}" =~ ^[0-9]+$ ]]; then
                BUMP_PR_NUM="${candidate}"
                REVISION_SHA="${sha}"
                break
              fi
            done
          fi

          if ! [[ "${BUMP_PR_NUM}" =~ ^[0-9]+$ ]]; then
            echo "::error::Unable to determine version-bump PR number for ${SOURCE_SHA} (${SOURCE_REF})."
            exit 1
          fi

          if [ "${CHANNEL}" = "main" ]; then
            VERSION_NAME="${BASE_VERSION}"
          else
            VERSION_NAME="${BASE_VERSION}-${CHANNEL}"
          fi

          BUILD_NUMBER="${BUMP_PR_NUM}"
          BUILD_NUMBER_RULE="version_bump_pr_number"

          {
            echo "base_version=${BASE_VERSION}"
            echo "version_name=${VERSION_NAME}"
            echo "build_number=${BUILD_NUMBER}"
            echo "release_tag=build-${CHANNEL}-v${VERSION_NAME}+${BUILD_NUMBER}"
            echo "release_name=${CHANNEL} v${VERSION_NAME} (${BUILD_NUMBER})"
            echo "pr_number=${BUMP_PR_NUM}"
            echo "source_sha=${SOURCE_SHA}"
          } >> "$GITHUB_OUTPUT"

          {
            echo "## Version metadata"
            echo ""
            echo "| Field | Value |"
            echo "|---|---|"
            echo "| channel | ${CHANNEL} |"
            echo "| source_ref | ${SOURCE_REF} |"
            echo "| source_sha | ${SOURCE_SHA} |"
            echo "| revision_sha | ${REVISION_SHA} |"
            echo "| version_bump_pr | #${BUMP_PR_NUM} |"
            echo "| base_version | ${BASE_VERSION} |"
            echo "| version_name | ${VERSION_NAME} |"
            echo "| build_number | ${BUILD_NUMBER} |"
            echo "| build_number_rule | ${BUILD_NUMBER_RULE} |"
          } >> "$GITHUB_STEP_SUMMARY"
