name: minglit_lints
description: Custom lint rules for Minglit project to enforce UI consistency and design tokens.
version: 0.0.4
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: ^8.0.0
  custom_lint_builder: ^0.8.1

custom_lint_plugin:
  entry_point: lib/minglit_lints.dart